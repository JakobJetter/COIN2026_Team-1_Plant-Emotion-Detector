# Virtual Environment 
.venv/ venv/ 

# Python cache 
__pycache__/ 

# Jupyter Notebook checkpoints 
.ipynb_checkpoints/


// Data
.plant_data/
.sessions/
sessions/            # matches at any depth, incl. data/sessions/ - raw recordings, not tracked
# data/datasets/ is intentionally NOT ignored - computed datasets (consolidated_dataset.csv,
# correlation_results.csv, etc.) are tracked so results stay reproducible without re-running everything.
*.mp4