# ══════════════════════════════════════════════════════════════
# init_recording.ipynb + ground_truth_classifier.ipynb
# Requirements
# ══════════════════════════════════════════════════════════════

# ── Core (always needed, both notebooks) ───────────────────────
numpy>=1.24,<3.0
pandas>=2.0
matplotlib>=3.7
seaborn>=0.13
scipy>=1.11
scikit-learn>=1.4
xgboost>=2.1

# ── Jupyter ────────────────────────────────────────────────────
jupyter>=1.0
ipykernel>=6.25
notebook>=7.0

# ══════════════════════════════════════════════════════════════
# Hardware-side requirements (real recording, not mock)
# ══════════════════════════════════════════════════════════════

# Polar H10 via BLE
bleak>=0.21

# Camera + HSEmotion FER
opencv-python>=4.8
hsemotion-onnx>=0.3.1      # CPU, recommended
# hsemotion>=0.4            # PyTorch variant (heavier)
# torch>=2.1                # only if you use hsemotion instead of hsemotion-onnx

# ESP32 plant-voltage + VOC streamer (WebSocket client)
websockets>=11

# CO2 sensor (SCD41) — placeholder, not yet wired.
# Uncomment once the sensor is connected to the host (NOT the ESP32):
# adafruit-circuitpython-scd4x
